// import Auth1Context from "../../../context/Auth1Context";
// import {useContext} from "react";

const Profile=() => {
    // let context= useContext(Auth1Context);
    // const [authUser, setAuthUser] = useState(null);
    //
    // useEffect(() => {
    //     setAuthUser(context.user)
    // }, [context]);

    return (
        <div className="profile w-full ">
        <div className="profile__image ">
            {/*<img src={profile.image} alt="profile image" />*/}
        </div>
        {/*<div className="profile__info">*/}
        {/*    <h2 className="profile__name">{authUser?.name}</h2>*/}
        {/*    <p className="profile__description">{profile.description}</p>*/}
        {/*</div>*/}
        </div>
    );
    };

export default Profile;
